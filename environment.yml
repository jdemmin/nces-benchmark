# environment.yml
name: nces-benchmark
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip>=24.0
  - openjdk=17
  - libstdcxx-ng
  - pip:
      - -r requirements.txt
      - -c constraints.txt